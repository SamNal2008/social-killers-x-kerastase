// Prompt generation for image generation

/**
 * Generates the prompt for image generation
 * Currently returns a hardcoded prompt
 * Future: Generate dynamic prompts based on user tribe/subculture
 */
export function generatePrompt(userResultId: string): string {
    // Hardcoded prompt as per requirements
    return "Take this picture and make him happy to code now";
}
